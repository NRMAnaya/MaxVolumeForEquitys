git init /////cuando una carpeta la quieres subir a git
git status /// nos 
git add (lo que quieras, un file pones el name, y . para subir todo)
git . ///subimos todo
git commit -am "Comentas lo que actualizaste"
git branch -M main  ///seleccionas a donde la vas a mandar osea la rama 
git remote add origin https://github.com/NRMAnaya/MaxVolumeForEquitys.git //pones el link a donde lo vas a subir
 git push -u origin main ///haces el push y lo subes

